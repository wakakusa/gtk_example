#include "headers.h"

typedef struct {
  GtkWidget *window1;
   gchar *file1;
  GtkTextBuffer *textbuf1;
  gchar *buf1;
  gsize size1;
  
}StructWindowWidget;

/*構造体変数名を宣言*/
  StructWindowWidget MainWindow; 
