#include "headers.h"

typedef struct {
  GtkWidget *window1;
   gchar *file1;
  GtkTextBuffer *textbuf;
  GtkTextView *textview;
  gchar *buf1;
  gsize size1;
}StructRScriptWidget;

/*構造体変数名を宣言*/
 StructRScriptWidget RScript1; 